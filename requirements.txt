# Wonk Chatbot - Production Requirements
# 基于实际部署配置，兼容Python 3.6+

# Flask Web框架 - 主要应用框架
Flask==2.0.3
Werkzeug==2.0.3
Jinja2==3.0.3
MarkupSafe==2.0.1
click==8.0.4
itsdangerous==2.0.1

# 基础依赖
python-dotenv==0.19.2
PyYAML==6.0
requests==2.27.1

# 数据处理
pandas==1.1.5
openpyxl==3.0.10

# 文本处理
jieba==0.42.1

# 日志
loguru==0.5.3

# 数据模型 - Python 3.6兼容版本
pydantic==1.9.2

# 开发和测试工具（可选）
pytest==7.0.1
httpx==0.23.3

# 注意：
# - 此配置已在阿里云Linux Python 3.6.8环境测试通过
# - 如需更新版本，请确保兼容性测试
# - FastAPI相关依赖已移除，项目使用Flask作为主要框架
