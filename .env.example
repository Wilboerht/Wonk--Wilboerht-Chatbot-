# Wonk Chatbot 环境变量配置示例
# 复制此文件为 .env 并根据需要修改

# 管理员Token（保护 /api/ingest 和 /api/config 接口）
# 生产环境请务必设置为强密码
WONK_ADMIN_TOKEN=your-secure-admin-token-here

# 配置文件路径（可选）
WONK_CONFIG=config.yaml

# FastEmbed 模型设置（可选）
# 中文优先：BAAI/bge-small-zh-v1.5（默认）
# 英文优先：BAAI/bge-small-en-v1.5
# 多语言：intfloat/multilingual-e5-small
WONK_FE_MODEL=BAAI/bge-small-zh-v1.5

# 日志级别（可选）
LOG_LEVEL=INFO

# 服务配置（可选，通常在 config.yaml 中设置）
# HOST=127.0.0.1
# PORT=8000
